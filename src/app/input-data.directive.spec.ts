import { InputDataDirective } from './input-data.directive';

describe('InputDataDirective', () => {
  it('should create an instance', () => {
    const directive = new InputDataDirective();
    expect(directive).toBeTruthy();
  });
});
